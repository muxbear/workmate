1. 我现在要开发一个通用智能体项目，采用前后端分离的设计，当前所在的目录是项目根目录，请根据以下结构创建项目目录，注意不要创建多余的目录或文件
ke-hermes/
  ├── frontend/
  │   ├── src/          # 前端源码
  │   ├── tests/        # 前端测试
  │   └── docs/         # 前端文档
  ├── backend/
  │   ├── src/          # 后端源码
  │   ├── tests/        # 后端测试
  │   └── docs/         # 后端文档
  └── docs/             # 总项目文档

2. 我已经在后端项目目录 backend/ 下创建了基本的项目代码，请按如下指令初始后端项目：
   1. 使用 uv 为它创建一个虚拟环境
   2. 激活这个虚拟环境
   3. 使用 uv add 命令安装所必须的依赖 python-dotenv, deepagents, langchain-openai, fastapi, uvicorn
3. 在后端项目 backend/docs 目录下一份后端的需求文档，需要实现一下功能
   1. 开发一个 uvicorn 服务，用于发布后端服务
   2. 统一使用阿里云百炼平台提供的模型，比如：大语言模型 deepseek-v4-pro, 文本向量 text-embedding-v4 等
   3. 在 backend/agent/graph.py 中使用 deepagents 框架实现一个智能体
   4. 在 backend/api/ 目录下为这个智能体发布相应接口
   请基于以上需求写需求文档